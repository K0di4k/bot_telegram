services:
  - type: web
    name: bot-telegram-python # Cambia el nombre si lo deseas
    env: python
    region: oregon # Elige la región que prefieras
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    routes:
      - path: /
        target: /
